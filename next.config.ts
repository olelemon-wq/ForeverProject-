import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js "N" badge; it can leave a large empty white panel while scrolling.
  devIndicators: false,
};

export default nextConfig;
